services:
  jellyfin:
    image: jellyfin/jellyfin:10.11.11
    container_name: jellyfin-mdblist-dev
    ports:
      - "8096:8096"
    volumes:
      - ./config:/config
      - ./cache:/cache
      - ./media:/media:ro
    restart: unless-stopped
